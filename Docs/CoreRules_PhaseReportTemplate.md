# CoreRules Phase Report Template

后续 CoreRules 阶段完成或独立验收时，优先使用本模板。按阶段实际情况保留“不适用”说明即可。

## 阶段名称

- 阶段：
- 目标：
- 风险级别：低 / 中 / 中高 / 高

## 修改范围

- 修改文件：
  - 
- 是否只修改允许范围：
- 是否修改 `FMCodex.Build.cs`：
- 是否触碰 `Content/`：
- 是否触碰 `Config/`：
- 是否触碰 UI：
- 是否触碰蓝图：
- 是否触碰联网：
- 是否触碰 Steam：

## 新增结构 / 新增接口

- 新增结构：
  - 
- 新增接口：
  - 
- 结果结构字段：
  - 
- 错误码：
  - 

## 调用与复用

- 调用了哪些已有模块：
  - 
- 是否重复实现已有逻辑：
- 如有重复实现，原因：
- 是否调用任何会修改状态的 Flow / Resolver：
- 是否做完整比赛循环：

## 状态与边界

- 是否修改输入状态：
- 是否通过 Updated 状态返回变化：
- 失败路径是否原子：
- 是否引入随机数：
- 是否引入抽牌 / 洗牌 / 牌库顶 / 初始发牌：
- 是否引入技能 / 卡牌数据库 / 球员属性读取：
- 是否做 UI / 蓝图 / 联网 / Steam：

## 新增测试

- 新增测试列表：
  - 
- 覆盖成功路径：
- 覆盖失败路径：
- 覆盖输入不变：
- 覆盖失败原子性：
- 覆盖边界约束：

## 构建与测试

- UnrealHeaderTool 结果：
- UE5 Development Editor 编译结果：
- CoreRules 自动化测试结果：
- 是否存在失败测试：

## 风险与结论

- 风险级别：低 / 中 / 中高 / 高
- 风险说明：
- 最终结论：通过 / 有条件通过 / 不通过
- 是否建议 commit：是 / 否
- 推荐 commit message：
  - 

